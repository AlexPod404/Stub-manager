# Руководство по слиянию в основную ветку (Merge Guide)

## Текущий статус

✅ **Ветка создана**: `copilot/create-initial-project-structure`  
✅ **Все изменения отправлены** (pushed) в удаленный репозиторий  
✅ **Pull Request готов** к проверке и слиянию  

## Способы слияния (Merge Options)

### 📌 Способ 1: Слияние через GitHub Web Interface (Рекомендуется)

Это самый простой и безопасный способ:

1. **Откройте Pull Requests на GitHub**
   ```
   https://github.com/AlexPod404/Stub-manager/pulls
   ```

2. **Найдите ваш Pull Request**
   - Название: "Initial project structure for Stub Manager"
   - Ветка: `copilot/create-initial-project-structure` → `main`

3. **Проверьте изменения**
   - Просмотрите вкладку "Files changed"
   - Убедитесь, что все 71+ файлов корректны

4. **Нажмите "Merge pull request"**
   - Выберите тип слияния:
     - "Create a merge commit" (создать commit слияния)
     - "Squash and merge" (объединить commits)
     - "Rebase and merge" (перебазировать)

5. **Подтвердите слияние**
   - Нажмите "Confirm merge"

6. **Готово!** 🎉
   - Ваши изменения теперь в основной ветке `main`

### 📌 Способ 2: Локальное слияние через командную строку

Если вы хотите слить локально:

```bash
# 1. Получить последние изменения из удаленного репозитория
git fetch origin

# 2. Переключиться на основную ветку main
git checkout main
# или создать локальную ветку main, если её нет
git checkout -b main origin/main

# 3. Обновить main до последней версии
git pull origin main

# 4. Слить вашу ветку в main
git merge copilot/create-initial-project-structure

# 5. Отправить изменения в удаленный репозиторий
git push origin main
```

### 📌 Способ 3: Использование GitHub CLI

Если у вас установлен GitHub CLI:

```bash
# Создать PR (если ещё не создан)
gh pr create --title "Initial project structure" --body "Complete Stub Manager architecture"

# Слить PR
gh pr merge --merge
```

## Что происходит после слияния

После слияния PR в ветку `main`:

1. ✅ Все 71+ файлов появятся в основной ветке
2. ✅ Структура проекта будет доступна для всех
3. ✅ Можно начинать разработку функционала
4. ✅ Можно клонировать репозиторий и запускать проект

## Проверка после слияния

После слияния обновите локальную ветку main:

```bash
# Переключиться на main
git checkout main

# Получить последние изменения
git pull origin main

# Проверить, что все файлы на месте
ls -la

# Увидеть историю
git log --oneline -5
```

## Что делать после слияния

Теперь можно приступать к разработке:

```bash
# 1. Установить зависимости
cd backend && npm install
cd ../runtime && npm install
cd ../frontend && npm install

# 2. Запустить через Docker Compose
cd docker
docker-compose up -d

# 3. Или запустить каждый сервис отдельно
# Terminal 1 - Backend
cd backend && npm run start:dev

# Terminal 2 - Runtime
cd runtime && npm run start:dev

# Terminal 3 - Frontend
cd frontend && npm run dev
```

## Доступ к сервисам после запуска

- Frontend: http://localhost (или http://localhost:3000)
- Backend API: http://localhost:3000/api
- API Документация: http://localhost:3000/api/docs
- Runtime: http://localhost:3001
- Health Check: http://localhost:3000/health

## Важно! ⚠️

- Перед слиянием убедитесь, что все тесты проходят
- Проверьте код на наличие конфликтов
- После слияния можно удалить ветку feature:
  ```bash
  git branch -d copilot/create-initial-project-structure
  git push origin --delete copilot/create-initial-project-structure
  ```

## Нужна помощь?

Если возникли проблемы:
1. Проверьте статус: `git status`
2. Посмотрите ветки: `git branch -a`
3. Проверьте логи: `git log --oneline -10`

---

**Статус**: Pull Request готов к слиянию ✅  
**Ветка**: copilot/create-initial-project-structure → main  
**Файлов**: 71+  
**Commits**: 3
