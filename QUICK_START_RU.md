# 🚀 Быстрый старт - Stub Manager

## ⚡ Самый быстрый способ получить проект

### На вашем компьютере откройте терминал и выполните:

```bash
# 1. Клонировать репозиторий
git clone https://github.com/AlexPod404/Stub-manager.git

# 2. Перейти в папку проекта
cd Stub-manager

# 3. Переключиться на ветку с новым функционалом (опционально)
git checkout copilot/create-initial-project-structure
```

**Готово!** Все файлы теперь у вас на компьютере в папке `Stub-manager/`

---

## 📦 Установка и запуск

### Вариант 1: С Docker (быстрее всего)

```bash
cd docker
docker-compose up -d
```

✅ Готово! Откройте http://localhost

### Вариант 2: Установка вручную

```bash
# Backend
cd backend
npm install
npm run start:dev

# В новом терминале - Runtime
cd runtime
npm install
npm run start:dev

# В новом терминале - Frontend
cd frontend
npm install
npm run dev
```

---

## 🌐 Ссылки после запуска

- Frontend: http://localhost
- Backend API: http://localhost:3000/api
- API Docs: http://localhost:3000/api/docs
- Runtime: http://localhost:3001

---

## 📚 Подробные инструкции

- **Установка на локальный компьютер**: см. `LOCAL_SETUP_RU.md`
- **Слияние в main ветку**: см. `MERGE_GUIDE_RU.md`
- **О файлах сборки (JAR и др.)**: см. `BUILD_ARTIFACTS_RU.md` ⚠️
- **Проверка установки**: см. `SETUP_VERIFICATION.md`
- **API документация**: см. `docs/API.md`
- **Архитектура**: см. `docs/ARCHITECTURE.md`

---

## ❓ Проблемы?

**Git не найден?**
```bash
# Установите Git
https://git-scm.com/downloads
```

**Node.js не установлен?**
```bash
# Установите Node.js 18+
https://nodejs.org/
```

**Docker не запускается?**
```bash
# Установите Docker Desktop
https://www.docker.com/products/docker-desktop/
```

---

## 📁 Что у вас теперь есть

```
Stub-manager/
├── backend/              ← NestJS сервер
├── runtime/              ← Runtime для заглушек
├── frontend/             ← React UI
├── docker/               ← Docker конфигурация
├── k8s/                  ← Kubernetes манифесты
├── docs/                 ← Документация
└── README.md             ← Главная документация
```

**71+ файлов** готовой структуры проекта! 🎉

---

## 🔄 Обновить проект позже

```bash
cd Stub-manager
git pull origin copilot/create-initial-project-structure
```

---

**Успехов в разработке!** 🚀
