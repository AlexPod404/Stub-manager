# 🔍 О файлах сборки проекта (Build Artifacts)

## ❗ Важно: JAR файлов в этом проекте нет

**Этот проект написан на Node.js/TypeScript, а НЕ на Java!**

JAR (Java ARchive) файлы используются только в Java проектах.  
В Node.js проектах используются другие форматы.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📦 Что используется в этом проекте вместо JAR

### Технологический стек:

- **Backend**: NestJS (Node.js + TypeScript)
- **Runtime**: Express (Node.js + TypeScript)
- **Frontend**: React + TypeScript + Vite

### Артефакты сборки (Build Artifacts):

┌─────────────────────────────────────────────────────────────┐
│ Backend (NestJS)                                            │
├─────────────────────────────────────────────────────────────┤
│ Папка: backend/dist/                                        │
│ Содержит: Скомпилированный JavaScript код                   │
│ Формат: .js файлы (не .jar)                                │
│                                                             │
│ Как собрать:                                                │
│   cd backend                                                │
│   npm install                                               │
│   npm run build                                             │
│                                                             │
│ Результат: backend/dist/ с .js файлами                     │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Runtime (Express)                                           │
├─────────────────────────────────────────────────────────────┤
│ Папка: runtime/dist/                                        │
│ Содержит: Скомпилированный JavaScript код                   │
│ Формат: .js файлы                                           │
│                                                             │
│ Как собрать:                                                │
│   cd runtime                                                │
│   npm install                                               │
│   npm run build                                             │
│                                                             │
│ Результат: runtime/dist/ с .js файлами                     │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Frontend (React)                                            │
├─────────────────────────────────────────────────────────────┤
│ Папка: frontend/dist/                                       │
│ Содержит: HTML, CSS, JavaScript для браузера                │
│ Формат: .html, .js, .css файлы                             │
│                                                             │
│ Как собрать:                                                │
│   cd frontend                                               │
│   npm install                                               │
│   npm run build                                             │
│                                                             │
│ Результат: frontend/dist/ со статическими файлами          │
└─────────────────────────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 🚀 Как собрать проект (вместо JAR)

### Вариант 1: Сборка всех компонентов вручную

```bash
# Backend
cd backend
npm install
npm run build
# Результат: backend/dist/

# Runtime
cd ../runtime
npm install
npm run build
# Результат: runtime/dist/

# Frontend
cd ../frontend
npm install
npm run build
# Результат: frontend/dist/
```

### Вариант 2: Использовать Docker (Рекомендуется)

Docker создаст готовые **Docker Images** (аналог JAR для контейнеров):

```bash
# Собрать Docker образы
cd docker

# Backend image
docker build -f backend.Dockerfile -t stub-manager-backend:latest ../backend

# Runtime image
docker build -f runtime.Dockerfile -t stub-manager-runtime:latest ../runtime

# Frontend image
docker build -f frontend.Dockerfile -t stub-manager-frontend:latest ../frontend

# Или собрать все сразу через docker-compose
docker-compose build
```

**Результат**: Docker образы (images), которые можно развернуть где угодно.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📦 Артефакты развертывания

### Что можно развернуть:

1. **Docker Images** (вместо JAR):
   - `stub-manager-backend:latest`
   - `stub-manager-runtime:latest`
   - `stub-manager-frontend:latest`

2. **Node.js приложения**:
   - `backend/dist/` - запускается через `node dist/main.js`
   - `runtime/dist/` - запускается через `node dist/main.js`
   - `frontend/dist/` - статические файлы для Nginx

3. **Kubernetes манифесты**:
   - Находятся в папке `k8s/`
   - Готовы для деплоя в OpenShift/Kubernetes

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 🎯 Эквиваленты JAR в Node.js

| Java | Node.js/TypeScript | Stub Manager |
|------|-------------------|--------------|
| JAR файл | Docker Image | stub-manager-backend:latest |
| WAR файл | Docker Image | stub-manager-runtime:latest |
| Executable JAR | node dist/main.js | npm run start:prod |
| Maven build | npm run build | npm run build |
| java -jar app.jar | node dist/main.js | node dist/main.js |

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📁 Структура после сборки

```
Stub-manager/
├── backend/
│   ├── dist/                  ← Собранный backend (аналог JAR)
│   │   ├── main.js           ← Точка входа
│   │   └── modules/          ← Скомпилированные модули
│   └── node_modules/         ← Зависимости
├── runtime/
│   ├── dist/                  ← Собранный runtime
│   │   └── main.js
│   └── node_modules/
├── frontend/
│   ├── dist/                  ← Собранный frontend
│   │   ├── index.html
│   │   ├── assets/           ← JS, CSS, images
│   │   └── ...
│   └── node_modules/
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 🚀 Запуск собранного проекта

### После npm run build:

```bash
# Backend
cd backend
NODE_ENV=production node dist/main.js

# Runtime
cd runtime
NODE_ENV=production node dist/main.js

# Frontend (нужен веб-сервер)
cd frontend
# Установить веб-сервер
npm install -g serve
serve -s dist -p 80
```

### С Docker (проще):

```bash
cd docker
docker-compose up -d
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 💡 Часто задаваемые вопросы

**Q: Почему нет JAR файла?**  
A: Потому что проект написан на Node.js, а не на Java.

**Q: Как развернуть проект в продакшене?**  
A: Используйте Docker образы или Kubernetes манифесты из папки k8s/.

**Q: Что аналогично JAR в этом проекте?**  
A: Docker образы - они содержат все необходимое для запуска.

**Q: Можно ли создать один исполняемый файл?**  
A: Да, можно использовать pkg или nexe для создания standalone executable:
```bash
npm install -g pkg
pkg backend/dist/main.js --target node18-linux-x64
```

**Q: Где хранятся зависимости (аналог Maven .m2)?**  
A: В папках `node_modules/` в каждом проекте.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📊 Размеры артефактов

Примерные размеры после сборки:

- **backend/dist/**: ~2-5 MB (только код)
- **backend/node_modules/**: ~200-300 MB (зависимости)
- **runtime/dist/**: ~1-3 MB
- **runtime/node_modules/**: ~100-150 MB
- **frontend/dist/**: ~5-10 MB (готовые static файлы)

Docker образы:
- **Backend image**: ~300-400 MB
- **Runtime image**: ~150-200 MB
- **Frontend image**: ~50-100 MB (с Nginx)

Для сравнения, Java JAR может быть 50-500 MB в зависимости от зависимостей.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 🎁 Резюме

**Вместо JAR файла в этом проекте используется:**

✅ Docker образы (для контейнерного развертывания)  
✅ Папки dist/ с скомпилированным JavaScript  
✅ Kubernetes манифесты (для облачного развертывания)  
✅ npm пакеты с зависимостями  

**Для развертывания:**
```bash
docker-compose up -d
```

**Для локальной разработки:**
```bash
npm run start:dev
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Если вам нужен именно Java/JAR вариант этого проекта, его нужно будет переписать на Spring Boot или другом Java фреймворке. Текущий проект - это Node.js/TypeScript.
