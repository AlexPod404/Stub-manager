# Инструкция: Как получить проект на локальный компьютер

## 🎯 Цель
Скопировать все файлы проекта Stub Manager в локальную папку на вашем компьютере.

## 📋 Предварительные требования

Убедитесь, что у вас установлены:
- ✅ Git (проверьте: `git --version`)
- ✅ Node.js 18+ (проверьте: `node --version`)
- ✅ npm (проверьте: `npm --version`)

Если чего-то нет:
- Git: https://git-scm.com/downloads
- Node.js: https://nodejs.org/

## 🚀 Способ 1: Клонирование репозитория (Рекомендуется)

### Шаг 1: Откройте терминал/командную строку

**Windows:**
- Нажмите `Win + R`
- Введите `cmd` или `powershell`
- Нажмите Enter

**Mac/Linux:**
- Нажмите `Cmd + Space` (Mac) или `Ctrl + Alt + T` (Linux)
- Введите `terminal`

### Шаг 2: Перейдите в нужную папку

```bash
# Например, перейти в папку Documents
cd Documents

# Или создать новую папку
mkdir Projects
cd Projects
```

### Шаг 3: Клонировать репозиторий

```bash
git clone https://github.com/AlexPod404/Stub-manager.git
```

Это создаст папку `Stub-manager` с полной копией проекта.

### Шаг 4: Перейти в папку проекта

```bash
cd Stub-manager
```

### Шаг 5: Проверить содержимое

```bash
# Посмотреть файлы
ls -la

# Должны увидеть:
# - backend/
# - runtime/
# - frontend/
# - docker/
# - k8s/
# - docs/
# - README.md
# и другие файлы
```

### Шаг 6: Переключиться на нужную ветку (если нужно)

```bash
# Посмотреть все ветки
git branch -a

# Переключиться на feature ветку с новым функционалом
git checkout copilot/create-initial-project-structure

# Или остаться на main ветке
git checkout main
```

## 🔄 Способ 2: Скачать ZIP архив

Если не хотите использовать Git:

### Вариант A: Скачать main ветку

1. Откройте в браузере:
   ```
   https://github.com/AlexPod404/Stub-manager
   ```

2. Нажмите зеленую кнопку **"Code"**

3. Выберите **"Download ZIP"**

4. Распакуйте архив в нужную папку

### Вариант B: Скачать feature ветку

1. Откройте в браузере:
   ```
   https://github.com/AlexPod404/Stub-manager/tree/copilot/create-initial-project-structure
   ```

2. Нажмите **"Code"** → **"Download ZIP"**

3. Распакуйте в нужную папку

## 📦 После получения файлов: Установка зависимостей

### Вариант 1: Установить все зависимости вручную

```bash
# В корневой папке проекта

# 1. Backend
cd backend
npm install
cd ..

# 2. Runtime
cd runtime
npm install
cd ..

# 3. Frontend
cd frontend
npm install
cd ..
```

### Вариант 2: Использовать Docker (проще)

Если у вас установлен Docker:

```bash
# В корневой папке проекта
cd docker
docker-compose up -d
```

Это автоматически:
- Установит все зависимости
- Запустит PostgreSQL
- Запустит Redis
- Запустит Backend
- Запустит Runtime
- Запустит Frontend

## 🎮 Запуск проекта локально

### Вариант 1: С Docker (Рекомендуется)

```bash
# В папке docker
cd docker
docker-compose up -d

# Проверить что все запустилось
docker-compose ps

# Остановить все сервисы
docker-compose down
```

### Вариант 2: Запустить каждый сервис отдельно

Откройте 3 терминала:

**Терминал 1 - Backend:**
```bash
cd backend
npm run start:dev
```

**Терминал 2 - Runtime:**
```bash
cd runtime
npm run start:dev
```

**Терминал 3 - Frontend:**
```bash
cd frontend
npm run dev
```

## �� Доступ к приложению

После запуска откройте в браузере:

- **Frontend**: http://localhost (или http://localhost:3000)
- **Backend API**: http://localhost:3000/api
- **API Документация**: http://localhost:3000/api/docs
- **Runtime**: http://localhost:3001
- **Health Check**: http://localhost:3000/health

## 📁 Структура папок в локальной копии

После клонирования вы увидите:

```
Stub-manager/
├── backend/              # NestJS Backend
│   ├── src/
│   │   ├── modules/     # Модули приложения
│   │   ├── main.ts      # Точка входа
│   │   └── ...
│   └── package.json
├── runtime/              # Runtime сервис
│   ├── src/
│   └── package.json
├── frontend/             # React Frontend
│   ├── src/
│   └── package.json
├── docker/               # Docker конфигурация
│   └── docker-compose.yml
├── k8s/                  # Kubernetes манифесты
├── docs/                 # Документация
├── README.md            # Главная документация
└── SETUP_VERIFICATION.md # Проверочный список
```

## 🔧 Настройка окружения

### 1. Создать .env файл для backend

```bash
cd backend
cp .env.example .env
```

### 2. Отредактировать .env (если нужно)

Откройте файл `backend/.env` в текстовом редакторе и измените:

```env
# Если используете локальную БД
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=stub_manager
DB_PASSWORD=stub_manager_password
DB_DATABASE=stub_manager

# Если используете Docker
DB_HOST=postgres
DB_PORT=5432
...
```

## 🔄 Обновление локальной копии

Чтобы получить последние изменения из GitHub:

```bash
# Перейти в папку проекта
cd /path/to/Stub-manager

# Получить последние изменения
git fetch origin

# Обновить текущую ветку
git pull origin main

# Или обновить feature ветку
git pull origin copilot/create-initial-project-structure
```

## ❓ Частые проблемы

### Проблема: "git: command not found"
**Решение**: Установите Git с https://git-scm.com/downloads

### Проблема: "npm: command not found"
**Решение**: Установите Node.js с https://nodejs.org/

### Проблема: "Permission denied"
**Решение**: 
```bash
# Linux/Mac
sudo chown -R $USER:$USER .

# Windows - запустите терминал от администратора
```

### Проблема: Порт уже занят
**Решение**:
```bash
# Найти процесс на порту 3000
# Windows
netstat -ano | findstr :3000

# Mac/Linux
lsof -i :3000

# Убить процесс или изменить PORT в .env
```

## 📞 Нужна помощь?

Проверьте:
1. Git установлен: `git --version`
2. Node.js установлен: `node --version`
3. Находитесь в правильной папке: `pwd`
4. Файлы скопировались: `ls -la`

## ✅ Проверка успешной установки

Выполните команды:

```bash
# 1. Проверить что Git работает
git status

# 2. Проверить что файлы на месте
ls -la

# 3. Проверить что зависимости установились
cd backend && npm list
cd ../runtime && npm list
cd ../frontend && npm list
```

Если все команды выполнились без ошибок - установка прошла успешно! 🎉

---

**Теперь у вас полная копия проекта на локальном компьютере!**

Можете:
- ✅ Изменять код
- ✅ Запускать проект локально
- ✅ Тестировать функционал
- ✅ Создавать новые feature
